/**
 * `/kirish` ostidagi sahifalar o'z qobig'ini o'zi tanlaydi:
 * kirish formasi — `AuthShell` (ikki ustun), rol tanlash va anketa —
 * keng bir ustunli sahifa. Shuning uchun bu yerda qo'shimcha o'ram yo'q.
 */
export default function KirishLayout({ children }: LayoutProps<"/kirish">) {
    return children;
}
